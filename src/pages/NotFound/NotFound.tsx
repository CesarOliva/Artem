import Error from '../../components/Error';

const NotFound = () => {
    return (
        <Error title="Ruta No Encontrada" text="La ruta que estás buscando no existe o ha sido eliminada." />
    );
}
 
export default NotFound;